# runtime/build artifacts
config/.build.env
*.swp
*~

# NEVER commit proprietary firmware (extract from Windows at install time)
*.mbn
*.elf
tcblaunch.exe
firmware/**/*.bin

# SSC sensor data — Qualcomm registry/config/RFSA fw + the Microsoft-derived
# secure-DB seed. Device/vendor data: captured locally, NEVER committed.
ssc-data/
sns-secure-db-seed.bin
sns_secure_database.bin

# slbounce EFI drivers are built from the slbounce project, not committed
config/slbounce/*.efi

# kernel build output / cloned source
*.dtb
Image
zenbook-a14-kernel/
