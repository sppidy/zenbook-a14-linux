# iris HW video codec hard-resets the A14: the VPU is TME-locked to a secure
# owner (Windows), no VPU PAS PD is exposed to Linux (qcom_scm_pas_init_image
# -22 at EL1 and EL2), and even no-TZ HW access trips a secure reset.
# Confirmed 2026-06-05. SW video decode is used instead.
blacklist qcom_iris
