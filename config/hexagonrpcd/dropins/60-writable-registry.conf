[Service]
# Our patched daemon REGENERATES the registry (writes sns_secure_database.bin),
# and the seed ExecStartPre writes it too. The base unit ProtectSystem=strict makes
# /var/lib read-only -> both fail. Allow writes to the registry tree.
ReadWritePaths=/var/lib/droid-juicer/sensors
