# ASUS Zenbook A14: the only SSC light source is the camera-QSH "color" sensor,
# not a standard "ambient_light". Tell libssc (patched, SSC_LIGHT_DATA_TYPE-aware)
# to look it up under "color" so iio-sensor-proxy exposes it as the ALS.
[Service]
Environment=SSC_LIGHT_DATA_TYPE=color
